/*
 * Navigation is now embedded statically in every HTML page.
 * This legacy loader is intentionally inert so older page references cannot
 * create a second copy of the navigation.
 */
(function(){
  'use strict';
})();
